const K='journal-v1';let T=JSON.parse(localStorage.getItem(K)||'[]');date.value=new Date().toISOString().slice(0,10);function addTrade(){T.unshift({date:date.value,instrument:instrument.value,session:session.value,setup:setup.value,pnl:+pnl.value||0,r:+r.value||0,adherence:Math.max(0,Math.min(100,+adherence.value||0)),notes:notes.value});localStorage.setItem(K,JSON.stringify(T));render()}function render(){rows.innerHTML=T.map(t=>'<tr><td>'+t.date+'</td><td>'+t.instrument+'</td><td>'+t.setup+'</td><td>$'+t.pnl.toFixed(2)+'</td><td>'+t.r.toFixed(2)+'</td><td>'+t.adherence+'%</td></tr>').join('');count.textContent=T.length;net.textContent='$'+T.reduce((s,t)=>s+t.pnl,0).toFixed(2);winrate.textContent=(T.length?100*T.filter(t=>t.pnl>0).length/T.length:0).toFixed(1)+'%';avgR.textContent=(T.length?T.reduce((s,t)=>s+t.r,0)/T.length:0).toFixed(2);avgA.textContent=(T.length?T.reduce((s,t)=>s+t.adherence,0)/T.length:0).toFixed(1)+'%'}render();