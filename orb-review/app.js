const K='orb-v1';let R=JSON.parse(localStorage.getItem(K)||'[]');date.value=new Date().toISOString().slice(0,10);function now(){let x=[...document.querySelectorAll('.c')];return Math.round(100*x.filter(v=>v.checked).length/x.length)}document.querySelectorAll('.c').forEach(x=>x.onchange=()=>score.textContent=now()+'%');function saveReview(){R.unshift({date:date.value,score:now(),lesson:lesson.value});localStorage.setItem(K,JSON.stringify(R));render()}function render(){score.textContent=now()+'%';count.textContent=R.length;let s=R.slice(0,7);avg.textContent=(s.length?s.reduce((a,x)=>a+x.score,0)/s.length:0).toFixed(1)+'%';rows.innerHTML=R.map(x=>'<tr><td>'+x.date+'</td><td>'+x.score+'%</td><td>'+x.lesson+'</td></tr>').join('')}render();